components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer

  schemas:
    Error:
      type: object
      properties:
        message:
          type: string
          example: Internal server error
      required:
        - message

    Usuario:
      type: object
      properties:
        nome:
          type: string
        email:
          type: string
          format: email
        senha:
          type: string
          format: password
      required:
        - nome
        - email
        - senha

    RequestPostUsuarioBody:
      type: object
      properties:
        nome:
          type: string
        email:
          type: string
          format: email
        senha:
          type: string
          format: password
      required:
        - nome
        - email
        - senha

    RequestPostResetPasswordBody:
      type: object
      properties:
        token:
          type: string
        senha:
          type: string
          format: password
      required:
        - token
        - senha
